// app/mcp/connector.ts — MCP client the agent uses. Phase 3.
// listTools / callTool. action-class tools MUST round-trip through main's
// proposal/confirm flow before execution (confirmation-always — §6, FS-D).
export {};
