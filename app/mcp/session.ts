// app/mcp/session.ts — JSON-RPC over ws. Phase 3.
// Request timeout + max in-flight cap + pending-map cleanup on timeout/close
// (the raw export leaked resolvers — FS-D). Per-launch bearer token on every call.
export {};
